[package]
name = "kse-launcher"
version = "2.0.0"
authors = ["Kai Kramer <kaikramer@users.noreply.github.com>"]
edition = "2021"

[profile.release]
strip = "symbols"

[dependencies]
winapi = { version = "0.3.9", features = ["winuser"] }
